# 2026-09-08 网站图片与中心成员更新

用户已要求将本轮更新发布到既有 Sites `homepage`，正式域名 https://heoagroup.org/。

## 照片

- 全部成员照片按网站卡片尺度检查，33 张模糊照片采用保守 AI 清晰化；其中王冬复用已展示的试样。孙健、李丁额外修正原图比例。
- 宋超以及 7 张已清晰的原始照片只进行等比例缩小与 WebP 编码，不使用 AI。宋超所有共用成员卡均采用 `object-position: center top`，保留头顶。
- 共 41 张网页用照片，长边不超过 640px，共 717,980 字节。原图和生成大图保存在本地；AI 推断细节不能视为真实细节恢复。
- `portrait-media-v24.json` 保存逐人映射、尺寸、字节和处理方式。`lib/media-paths.ts` 为成员卡、正文、详情和旧链接提供共用解析。
- 赖泓宇没有原始头像，保留明确占位，不生成虚构头像。

## 体积

- 既有 3,618 个展示媒体中，1,089 张逐字节相同的图片合并到相同 URL；原有文章图片字节不变。
- 旧 320px 备用镜像不再随正式站重复发布；旧 URL 通过 308 跳转到现有清晰版本。
- HEOA 标志、中心标志、BSTVC 图标和二维码合计从 1,929,835 字节降至 310,756 字节，约减少 83.9%。二维码保持足够像素，不重绘。
- 全部静态媒体从 198,777,144 字节降至 116,166,610 字节，减少 82,610,534 字节（41.56%）。附件保留；文章图片已采用最大 1280px WebP，不再二次有损压缩。
- 原素材不删除。每次生成发布目录时，上一份暂存树保留在忽略的 `.media-stage-history` 内。

## 中心名单

HEOA 主名单仍为 62 位。中心完整目录从 13 位改为 15 位：移出樊洋涛、杨伊里，保留邹锟，增加汤万杰、王茂夏、赖泓宇、薛清萍。被移出人员仅归档其中心记录，原资料保留。中心首页继续展示原有 5 位核心成员。

## 验证

- 正式构建成功，媒体质量门禁通过：3,618 个媒体，3,542 张栅格图，3,616 个逻辑资产 ID；文章媒体构建字节保持一致。
- 修改涉及的 TS/TSX 定向 lint 通过。全站旧 lint 仍报告 language-menu 不可变性和 site-search autoFocus 两处既有问题，与本轮无关。
- 本地 14 个页面、103 张图片字节、4 个历史 URL 跳转核验通过；核对 62 位主站成员和 15 位中心成员。
- 浏览器检查孙健、李丁、杨练以及宋超的实际卡片，确认共用新 WebP 与头顶留白。
- 发布终态与正式域名线上核验记录由项目根目录 `上线记录.md` 提供。
